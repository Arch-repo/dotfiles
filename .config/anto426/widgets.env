# Set to 0 to prevent widgets from starting with Hyprland.
export ANTO426_WIDGETS_AUTOSTART=1

# Built-in widgets enabled by default: clock cava system.
# Leave empty to use only custom widgets from widgets_custom.env.
export ANTO426_WIDGETS_ENABLED=""
export ANTO426_WIDGETS_LOCKED=1
